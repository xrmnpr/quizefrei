PORT=3000
JWT_SECRET=change-this-to-a-long-random-secret
NODE_ENV=development

# PostgreSQL — set these in Azure App Settings (never commit real values)
# Supports both standard PG* names and Azure Service Connector naming
PGHOST=quizefre-server.postgres.database.azure.com
PGUSER=njboctripo
PGPASSWORD=your_password_here
PGDATABASE=quizefre-database
PGPORT=5432
